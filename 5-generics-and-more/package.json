{
  "devDependencies": {
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "@types/node": "^12.0.2",
    "axios": "^0.18.0",
    "concurrently": "^4.1.0",
    "json-server": "^0.15.1"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
